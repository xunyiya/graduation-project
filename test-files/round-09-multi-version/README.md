# Round 09 - 多版本对比样例

用于验证“多版本连续对比”功能。进入对比页后选择多个文件，系统会按文件名自然顺序生成连续版本链。

## Text

选择：

- `v1.txt`
- `v2.txt`
- `v3.txt`

预期：

- `v1 -> v2` 出现状态和接口地址修改
- `v2 -> v3` 出现备注替换为负责人

## JSON

选择：

- `v1.json`
- `v2.json`
- `v3.json`

预期：

- `v1 -> v2` 出现版本号修改、功能开关新增
- `v2 -> v3` 出现名称修改、监控配置新增
